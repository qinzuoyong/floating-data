# Project Rules: BatteryFloating

<project_context>
- **Target Project**: `C:\Users\yong\AndroidStudioProjects\BatteryFloating`
- **Core Directive**: 针对本项目的任何修改，必须严格遵守以下文档更新、版本控制、构建与实测协议。下文中的 `[禁止]` 为最高优先级强制指令，违反即视为任务失败。
</project_context>

## 1. 📝 README 文档更新协议
每次任务涉及代码新增或修改时，必须同步更新项目根目录下的 `readme.md` 文件。

**执行动作：**
- **精准修改**：仅允许增加或修改与当前任务直接相关的描述。`[禁止]` 修改、删除或重排任何与本次变更无关的原有内容，必须 100% 保持原样。
- **版本信息写入逻辑**（严格按条件分支执行）：
  1. **定位**：在 `readme.md` 中查找当前项目版本号对应的区块。
  2. **分支 A（无当前版本记录）**：若文件中尚未包含当前版本的改动信息，必须在**上一个版本区块的正上方**新增一行，写入本次变更内容。
  3. **分支 B（已有当前版本记录）**：若文件中已存在当前版本的改动信息，必须在**当前版本信息的末尾直接追加**本次变更内容。

## 2. 🔄 Git 版本控制协议
代码与文档更新完成后，必须执行以下 Git 工作流：

**执行动作：**
- **本地提交**：自动执行 `git add .` 和 `git commit -m "[描述]"`，将变更安全提交至本地仓库。
- `[禁止]` **自动远程推送**：绝对 `[禁止]` 执行 `git push` 或任何向远程仓库同步的操作。
- **推送触发条件**：远程推送操作**必须**等待用户明确下达指令（如包含“推送到远程”、“push”、“同步远端”等关键词）后方可执行。

## 3. 📦 APK 构建与核心一致性协议
涉及代码修改且需要重新构建 APK 时，必须确保新 APK 的核心标识与现有发布包完全一致，以保证可覆盖安装。

**绝对约束（未经明确许可，`[禁止]` 变更）：**
- `[禁止]` 修改 包名 / Application ID。
- `[禁止]` 更改 签名证书（keystore 文件路径、别名、密码）。
- `[禁止]` 修改 版本号（`versionCode`）和 版本名称（`versionName`）。
- *注：若因特殊需求必须变更上述信息，必须先向用户说明原因并获取明确的“允许修改”指令，否则一律保持原配置。*

**构建与校验工作流：**
1. **执行构建**：使用项目现有 Gradle 配置执行构建（如 `./gradlew assembleRelease` 或用户指定的变体），必须明确引用已配置的签名文件。
2. **自动校验**：构建完成后，必须通过命令行工具（如 `aapt dump badging` 或 `apksigner verify`）自动核对新 APK 的包名与签名信息。
3. **熔断机制**：若校验发现包名或签名与上一版本不一致，**立即终止任务**并向用户报告不一致详情，`[禁止]` 继续分发、安装或提交该 APK。

## 4. 🧪 IDE 实测与闭环验证协议 (Test-Fix-Retest Loop)
本版本全部功能必须在指定的 Android Studio 环境中进行实测验证，确保功能可用。

**指定测试环境：**
- **唯一 IDE 路径**：`D:\android-studio-quail1-windows\bin\studio64.exe`
- 所有编译、运行、模拟器启动及真机部署调试，必须基于此环境进行。

**执行动作与闭环机制：**
- **强制实测**：代码修改完成后，`[禁止]` 仅凭代码逻辑分析就声称“任务完成”。必须在上述指定环境中实际运行并验证修改的功能。
- **异常处理循环 (Test-Fix-Retest)**：
  若在实测中发现代码报错、编译失败、运行崩溃或功能不符合预期，必须严格执行以下循环：
  1. **暂停并分析**：立即停止当前操作，分析 Logcat 报错或异常原因。
  2. **重构计划**：输出 `🔧 修正计划: [具体修改步骤]`。
  3. **重新修改**：根据新计划修改代码。
  4. **再次实测**：重新在指定 IDE 环境中编译运行并验证。
- **退出条件**：上述循环必须持续执行，**直到修改的功能在指定环境中正常使用且无异常**。只有当实测通过后，才能进入 Git 提交流程。