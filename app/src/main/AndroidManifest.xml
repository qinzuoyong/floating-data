<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- 家人位置共享：定位权限（按需前台定位 + 后台常驻连接） -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 后台定位：前台服务被系统重启拉起(app 不在前台)时仍能定位应答家人请求 -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- signature 级权限,安装后默认拒绝;由内置 ADB 通道执行 pm grant 自授(无障碍自愈用),见 A11ySelfHealer -->
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <!-- Shizuku 桥接:声明 API 权限并挂 provider 接收 server 的 binder(方案一) -->
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:installLocation="auto"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BatteryFloating"
        android:appCategory="productivity"
        android:networkSecurityConfig="@xml/network_security_config">
        <!-- 高德定位SDK必需的后台定位服务(官方文档要求手动声明,AAR 不自动合并) -->
        <service android:name="com.amap.api.location.APSService" />
        <!-- 高德定位SDK密钥(11.x 只从 meta-data 读取,setApiKey 无效);值经 manifestPlaceholders 注入 -->
        <meta-data android:name="com.amap.api.v2.apikey" android:value="${amapKey}" />

        <activity
            android:name=".MainActivity"

            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.BatteryFloating"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".WebViewActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />

        <service
            android:name=".service.FloatingWindowService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="floating_window_battery_monitor" />
        </service>

        <!-- 家人位置共享服务(普通后台服务,非前台)：保持信令连接 + 按需响应定位请求;
             后台常驻由同进程悬浮窗前台服务保活,故不再声明 foregroundServiceType -->
        <service
            android:name=".service.FamilyLocationService"
            android:exported="false"
            android:stopWithTask="false" />

        <service
            android:name=".service.KeepAliveJobService"

            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <!-- 无障碍保活（GKD 同款机制）：系统绑定提升进程优先级 + 免广播开机自启通道 -->
        <service
            android:name=".service.KeepAliveAccessibilityService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/keep_alive_accessibility" />
        </service>

        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.example.batteryfloat.action.AUTO_START_CHECK" />
            </intent-filter>
        </receiver>

        <!-- ADB 无线调试配对前台服务:通知栏 RemoteInput 直回配对码(与系统配对弹窗同屏) -->
        <service
            android:name=".adb.AdbPairingService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="adb_wireless_pairing_interactive" />
        </service>

        <!-- FileProvider：用于分享下载的 APK 给系统安装器 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Shizuku provider:server 借此向本进程投递 binder(官方要求的标准声明) -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

    </application>

</manifest>